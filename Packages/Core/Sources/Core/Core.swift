/// Root namespace for the `Core` module.
///
/// Holds shared models, protocols and types reused across LifeView modules.
/// Intentionally empty in P0 — content lands in later phases.
public struct Core: Sendable {
    public init() {}
}
