/// Root namespace for the `DesignSystem` module.
///
/// Will host design tokens (colors, spacing, typography) and reusable
/// SwiftUI components. Empty in P0.
public struct DesignSystem: Sendable {
    public init() {}
}
